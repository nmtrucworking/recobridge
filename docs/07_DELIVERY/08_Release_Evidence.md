# Release Evidence — 2026-07-24

## Release decision

- Data profile: `release`, 20.000 users, seed 42.
- XGBRanker candidate: `xgb-20260724-release-v2`.
- Production mode: `baseline_fallback`.
- Production strategy: `category_popular`.
- Ranker promotion: blocked only by candidate Recall@200 (`0.064998 < 0.70`).
- Production alias: `apps/ml/artifacts/models/release/production.json`.

XGBRanker was trained successfully. It achieved validation NDCG@10 `0.036941`
and test NDCG@10 `0.033462`, but a ranker cannot recover a relevant product that
is absent from its bounded candidate pool. The project therefore follows the
governed fallback instead of mislabeling baseline output as XGBoost output.

## Evidence matrix

| Gate | Result | Evidence |
|---|---|---|
| Data Ready | PASS | `artifacts/data/release/{data_manifest,data_quality_report,split_manifest}.json` |
| Model Ready | PASS | release manifest, cluster/baseline/metrics reports, model files and checksum |
| Ranker Promote | BLOCKED | Recall@200 `0.064998`; remaining offline gates pass |
| Baseline Production | PASS | checksum-protected `production.json`, `category_popular` |
| API Ready | PASS | 9 FastAPI tests and production-alias loader test |
| Web Ready | PASS | lint, production build and 3 render/BFF tests |
| Performance | PASS | 1.000 ASGI requests; p95 `6.069 ms` ≤ `200 ms` |
| Live Smoke | PASS | two release users differ; anonymous fallback; BFF health `ok` |

## Verified presentation commands

```powershell
.\scripts\start-demo.ps1
.\scripts\smoke-demo.ps1
.\scripts\stop-demo.ps1
```

Expected release profiles:

- Profile A / `10002945`: `219232, 1243077, 561077, 52586`.
- Profile B / `10005456`: `1068914, 1086081, 219232, 609119`.
- Anonymous: `recent_popular`, `degraded=true`.

The exact order can change after retraining; the smoke assertion checks that the
two known users differ and that the cold-start contract remains truthful.

## Presentation wording

Say: “XGBRanker trained and beat the baseline among retrieved candidates, but
candidate recall failed the release gate, so production uses the strongest
validated baseline while the ranker remains versioned as a candidate.”

Do not say: “Production recommendations are generated by XGBoost.”
